{
  "manifestVersion": 2,
  "id": "sun-study",
  "name": "Sun and Shadow",
  "version": "1.0.0",
  "sdk": ">=2.0.0 <3",
  "description": "Real sun direction by date, time and place, and sunlight hours accumulated on a chosen surface by ray casting.",
  "runtime": { "kind": "bundled", "entry": "panel.ts" },
  "activationEvents": ["onPanel:sun-study"],
  "permissions": [
    "model.summary.read",
    "model.structure.read",
    "model.properties.read",
    "geometry.query",
    "geometry.mesh.read",
    "view.read",
    "view.control",
    "storage.extension",
    "file.export"
  ],
  "contributes": {
    "panels": [{ "id": "sun-study", "title": "Sun and Shadow", "icon": "globe" }],
    "exporters": [{ "id": "sun-study.csv", "title": "Sunlight hours CSV", "mimeTypes": ["text/csv"] }]
  },
  "catalog": {
    "tagline": "Sun position by date and place, and sunlight hours on a surface",
    "about": "The NOAA solar position algorithm drives the model's light from an explicit site time offset. The accumulation ray-casts a documented grid against the real mesh rather than sampling a shadow map, making it useful for repeatable early-stage sunlight comparisons.",
    "icon": "globe",
    "category": "Analysis",
    "keywords": "sun shadow solar daylight sunlight hours overshadowing right to light planning study season equinox solstice",
    "does": [
      "Light the model from the real sun for any date, time and place",
      "Read the site's latitude and longitude from the IFC where they were written",
      "Accumulate sunlight hours over a whole day on a site grid or the selection",
      "Colour the model by the hours each element receives, and export the grid"
    ]
  }
}
